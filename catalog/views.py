from django.shortcuts import render
from django.http import HttpResponse
from .models import Movies, Cinema, ShowTiming, BookingData, Auditorium
from datetime import datetime

# Create your views here.
